package arlcm;

struct navigator_plan_t {
    int64_t utime;

    int32_t num_waypoints;
    waypoint_id_t waypoints[num_waypoints];

    int32_t checkpoint_index;
    int32_t checkpoints_remaining;
}
